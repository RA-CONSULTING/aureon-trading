# Aureon — Linux-safe dependency set (the full system, minus desktop-only deps).
# ─────────────────────────────────────────────────────────────────────────────
# This is requirements.txt with the Windows-only / X11-desktop-control packages
# removed, so `pip install -r requirements-linux.txt` succeeds on a headless Linux
# server. The full trading/scientific/exchange stack the swarm needs is kept.
#
# Excluded (desktop-control, optional — install by hand on a Linux desktop if you
# actually want SafeDesktopControl / OCR / audio):
#   pycaw            → Windows-only (comtypes); pip install FAILS on Linux
#   opencv-python    → heavy X11 vision, not needed by the trading swarm
#   pytesseract      → needs the tesseract binary; OCR only
#   screeninfo       → X11 screen geometry
#   keyboard, mouse  → global input hooks (need root / X11)
#   pyautogui        → desktop automation (needs X11 + scrot)
#   PyAudio          → needs portaudio headers; mic capture only
#   sounddevice      → needs portaudio; audio only
# The organism/operator/HNC core installs from `pip install -e '.[operator]'`;
# this file adds the market/analytics/exchange deps for the full swarm.

requests>=2.32.0
python-dotenv>=1.0.0
click<8.2
python-binance>=1.0.19
krakenex>=2.1.0
numpy>=1.26.0
pandas>=2.1.0
matplotlib>=3.8.0
# --- rendering (bio harmonic overlays / convergence maps / blueprints) ---
Pillow>=10.0.0
imageio>=2.34.0
imageio-ffmpeg>=0.5.1
svgwrite>=1.4.0
aiohttp>=3.9.0
websockets>=11.0.3,<12.0
beautifulsoup4>=4.12.0
wikipedia>=1.4.0
wikipedia-api==0.6.0
skyfield
ephem
astroquery
astropy
scipy
cryptography
flask
psutil>=5.9.0
supervisor
rich>=13.7.0
alpaca-py>=0.20.0
pyttsx3>=2.90
redis>=4.5.0
websocket-client>=1.9.0
prometheus_client>=0.24.0
anthropic>=0.40.0
yfinance>=0.2.36,<0.2.59
fredapi>=0.5.2
pyperclip>=1.8.2
SpeechRecognition>=3.10.0
