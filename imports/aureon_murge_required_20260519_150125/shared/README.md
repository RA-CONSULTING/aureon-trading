# Shared

Reserved for future shared TypeScript models, command schemas, and runtime/client contracts.

Current implementation keeps the schema surface small and documented in `docs/ARCHITECTURE.md`.
