# Aureon Email Drafts

Generated local .eml draft files. No email has been sent.

- `00_Gary_Self_Update_All_PDFs.eml` is addressed to `gaxlec@gmail.com` and attaches all five PDFs.
- Route-specific drafts omit the `To` field so Gary can add the correct recipient before sending.
- Every draft includes the matching PDF attachment.
