"""``aureon.bio`` — bio-derived signal probes that reuse the falsifiable
phenolic-fingerprint engine.

This package holds modules that feed *derived* signals (never a raw biological
measurement, never a claim about a person) into the pre-registered statistical
machinery of ``phenolic_fingerprint.py``. The scientific boundary is load-bearing
and enforced in code: these modules test for statistical structure in a derived
signal representation only. They do not measure, diagnose, or assert anything
about a human being.
"""
