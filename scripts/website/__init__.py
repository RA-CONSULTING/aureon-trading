"""Tooling for the static `website/` site: audit, package, and FTP-deploy.

Pure standard library — no third-party dependency, no network at import time. See
`scripts/website/README.md` for the audit → build → deploy workflow.
"""
